#include <stdio.h>
//Escribir un programa en C que sume todos los n ́umeros enteros que ingresen por entrada est ́andar. Finalizar de leer n ́umeros al encontrar EOF (end of file). Imprimir la suma.

int main(void) {
  int n;
  int suma = 0;
  while (scanf(%d != )) {
    /* code */
  }
  return 0;
}
